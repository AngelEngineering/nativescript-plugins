<Page xmlns="http://schemas.nativescript.org/tns.xsd"
  navigatingTo="navigatingTo"
  loaded="onLoaded"
  class="page">
  <Page.actionBar>
    <ActionBar title="Nativescript Plugin Demos"
      icon=""
      class="action-bar"/>
  </Page.actionBar>
  <StackLayout class="p-20">
    <ScrollView class="h-full">
      <StackLayout>
        <Button text="flashlight"
          tap="{{ viewDemo }}" />
        <Button text="filepicker"
          tap="{{ viewDemo }}" />
        <Button text="downloader"
          tap="{{ viewDemo }}" />
        <Button text="audio-player"
          tap="{{ viewDemo }}" />
        <Button text="audio-recorder"
          tap="{{ viewDemo }}" />
        <Button text="sleepcontrol"
          tap="{{ viewDemo }}" />
        <Button text="camera"
          tap="{{ viewCamera }}" />
        <Button text="videoplayer"
          tap="{{ viewDemo }}" />
      </StackLayout>
    </ScrollView>
  </StackLayout>
</Page>