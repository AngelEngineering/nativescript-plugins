<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">
  <Page.actionBar>
    <ActionBar title="Plugin Demos" icon="" class="action-bar"/>
  </Page.actionBar>
  <StackLayout class="p-20">
    <ScrollView class="h-full">
      <StackLayout>
        <Button text="flashlight" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="filepicker" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="downloader" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="audio-player" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="audio-recorder" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
      </StackLayout>
    </ScrollView>
  </StackLayout>
</Page>